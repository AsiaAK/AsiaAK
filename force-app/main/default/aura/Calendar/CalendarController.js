({
	handleInitCmp: function(component, event, helper) {
		helper.initCmp(component);
    },

    handleEventsChange: function(component, event, helper) {
        helper.eventsChange(component);
    },

    handleResourcesChange: function(component, event, helper) {
        helper.resourcesChange(component);
    }
})